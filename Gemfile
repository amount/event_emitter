source 'https://rubygems.org'

git_source(:avant_repo) { |repo| "https://github.com/avantcredit/#{repo}.git" }

gem 'stathat-json', '~> 0.1', avant_repo: 'stathat-json'

# Specify your gem's dependencies in avant-event_emitter.gemspec
gemspec
